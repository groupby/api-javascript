import Actions from '../actions';
import Store from '../store';

export default function updateTemplate(state, action) {
  switch (action) {
    // case Actions.UPDATE_TEMPLATE:
    //   return { ...state };
    default:
      return state;
  }
}
